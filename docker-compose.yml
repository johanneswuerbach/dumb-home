version: '3'

services:
  prometheus:
    image: napnap75/rpi-prometheus:prometheus-2.3.2
    ports:
    - "9090:9090"
    volumes:
    - ${FULL_PATH}/prometheus.yml:/etc/prometheus/prometheus.yml
    - prom-data:/prometheus
  node-exporter:
    image: napnap75/rpi-prometheus:node_exporter-0.16.0
    expose:
    - "9100"
    volumes:
    - /proc:/host/proc:ro
    - /sys:/host/sys:ro
    - /:/rootfs:ro
    - /var/run/dbus:/var/run/dbus
    - /run/systemd:/run/systemd:ro
    command:
    - '--path.procfs=/host/proc' 
    - '--path.sysfs=/host/sys'
    - --collector.filesystem.ignored-mount-points
    - "^/(sys|proc|dev|host|etc|rootfs/var/lib/docker/containers|rootfs/var/lib/docker/overlay2|rootfs/run/docker/netns|rootfs/var/lib/docker/aufs)($$|/)"
  grafana:
    image: fg2it/grafana-armhf:v5.1.4
    volumes:
    - grafana-data:/var/lib/grafana
    - ${FULL_PATH}/grafana/dashboards:/var/lib/grafana/dashboards
    - ${FULL_PATH}/grafana/provisioning:/var/lib/grafana/provisioning
    ports:
    - "3000:3000"
    environment:
    - GF_PATHS_PROVISIONING=/var/lib/grafana/provisioning
  fritz-box-exporter:
    image: johanneswuerbach/fritzbox_exporter:1
    expose:
    - "9133"
  homematic-exporter:
    image: johanneswuerbach/homematic_exporter:1
    volumes:
    - /dumb-home/config:/usr/src/app/config:ro
    - /etc/timezone:/etc/timezone:ro
    expose:
    - "8000"

volumes:
  prom-data:
  grafana-data:
